Por favor coloca aquí la imagen que adjuntaste en el proyecto para usarla como logo y favicon.

Archivo encontrado actualmente:
- "Logo SIURTEC 1.png" (usar tal cual o renombrar a `siurtec.png` / `siurtec.avif` para evitar espacios)

Nombres recomendados y tamaños si prefieres renombrar:
- siurtec.avif     (opcional, AVIF preferido)
- siurtec.png      (fallback PNG, 192x192 o 512x512)
- siurtec-192.png  (192x192 para apple-touch y manifest)
- favicon.ico      (opcional, para compatibilidad)

Sugerencias:
- Renombrar `Logo SIURTEC 1.png` a `siurtec.png` evitará problemas con espacios en rutas y facilitará futuras referencias.
Sugerencias:
- Mantén una versión cuadrada de la imagen para favicons (ej. 192x192 y 48x48).
- Si quieres generar `favicon.ico`, puedes usar herramientas online (favicon generator) o convertir la PNG.

Después de añadir los archivos, recarga la página o limpia la caché para ver el favicon actualizado.